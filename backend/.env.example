# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Redis (optional). If omitted, caching falls back to in-memory no-op
# REDIS_URL=redis://localhost:6379

# App
PORT=3000
